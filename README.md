### 개발 환경 설정
```bash
brew install mongodb
brew services start mongodb
brew install nvm
vi ~/.bashrc or ~/.zshrc

export NVM_DIR="$HOME/.nvm"
. "/usr/local/opt/nvm/nvm.sh"

nvm install v7.7.4
npm install
npm run dev
```
